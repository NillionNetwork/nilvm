[package]
name = "client-fixture"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
rand = "0.8"
tracing = "0.1"
uuid = "1.16"

# Nillion dependencies
nillion-client = { path = "../../../client" }
mpc-vm = { path = "../../../libs/execution-engine/mpc-vm", features = ["simulator"] }
nada-value = { path = "../../../libs/nada-value" }
bytecode-evaluator = { path = "../../../nada-lang/bytecode-evaluator" }
nodes-fixtures = { path = "../nodes" }
math_lib = { path = "../../../libs/math" }
