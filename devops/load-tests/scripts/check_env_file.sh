#!/usr/bin/env bash

: "${GRAFANA_NILLION_NETWORK_UUID:?is required to run the load-tool pipeline}"
: "${GRAFANA_TOKEN:?is required to run the load-tool pipeline}"
: "${GRAFANA_URL:?is required to run the load-tool pipeline}"
: "${PRIVATE_KEY:?is required to run the load-tool pipeline}"
